<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="MainWindow"
        Title="Network Speed Logger"
        Width="1440" Height="960" MinWidth="1080" MinHeight="720"
        WindowStartupLocation="CenterScreen"
        Background="#F3F6FA"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
  <Window.Resources>
    <SolidColorBrush x:Key="Accent" Color="#4F7CFF"/>
    <SolidColorBrush x:Key="Download" Color="#4F7CFF"/>
    <SolidColorBrush x:Key="Upload" Color="#20B486"/>

    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="White"/>
      <Setter Property="BorderBrush" Value="#E3E9F2"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="16"/>
      <Setter Property="Padding" Value="20"/>
    </Style>

    <Style x:Key="SidebarLabel" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#AAB6C8"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Margin" Value="0,0,0,7"/>
    </Style>

    <Style x:Key="InputTextBox" TargetType="TextBox">
      <Setter Property="Height" Value="38"/>
      <Setter Property="Background" Value="#1E2B3E"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="CaretBrush" Value="White"/>
      <Setter Property="BorderBrush" Value="#35445A"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="11,8"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SidebarCombo" TargetType="ComboBox">
      <Setter Property="Height" Value="38"/>
      <Setter Property="Background" Value="#F8FAFD"/>
      <Setter Property="Foreground" Value="#1F2B3D"/>
      <Setter Property="BorderBrush" Value="#65738A"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="8,5"/>
      <Setter Property="ItemContainerStyle">
        <Setter.Value>
          <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#182232"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="9,7"/>
          </Style>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SidebarAdapterList" TargetType="ListBox">
      <Setter Property="Background" Value="#172437"/>
      <Setter Property="Foreground" Value="#F4F7FB"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="2"/>
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
      <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBox">
            <Border Background="#172437" CornerRadius="7" ClipToBounds="True">
              <ScrollViewer Background="#172437" Focusable="False" Padding="2" CanContentScroll="True">
                <ItemsPresenter/>
              </ScrollViewer>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="ItemContainerStyle">
        <Setter.Value>
          <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#F4F7FB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Focusable" Value="False"/>
          </Style>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="BaseButton" TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="Background" Value="#4F7CFF"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,10"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="9" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.88"/></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.72"/></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.38"/><Setter Property="Cursor" Value="Arrow"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
      <Setter Property="Background" Value="#2A394E"/>
      <Setter Property="Foreground" Value="#E8EEF7"/>
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
      <Setter Property="Background" Value="#E45858"/>
    </Style>

    <Style x:Key="LightButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
      <Setter Property="Background" Value="#EDF2FF"/>
      <Setter Property="Foreground" Value="#3159C8"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="#F7F9FC"/>
      <Setter Property="Foreground" Value="#768399"/>
      <Setter Property="FontSize" Value="11"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="10,9"/>
      <Setter Property="BorderBrush" Value="#E8EDF4"/>
      <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <Style TargetType="DataGridCell">
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="10,8"/>
      <Setter Property="Foreground" Value="#334155"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style TargetType="DataGridRow">
      <Setter Property="Background" Value="White"/>
      <Setter Property="BorderBrush" Value="#EEF2F7"/>
      <Setter Property="BorderThickness" Value="0,0,0,1"/>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#F8FAFD"/></Trigger>
      </Style.Triggers>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="330"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0" Background="#111B2A">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Padding="25,23,23,20" BorderBrush="#2A3648" BorderThickness="0,0,0,1">
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="46"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Width="38" Height="38" CornerRadius="11" Background="#4F7CFF" VerticalAlignment="Center">
              <TextBlock Text="↕" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
              <TextBlock x:Name="AppTitleText" Text="网速记录工具" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
              <TextBlock Text="Network Speed Logger" Foreground="#8492A6" FontSize="11" Margin="0,4,0,0"/>
            </StackPanel>
          </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel x:Name="ConfigInputs" Margin="23,20,23,16">
            <TextBlock x:Name="SettingsTitleText" Text="记录设置" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <TextBlock x:Name="LanguageLabel" Text="界面语言" Style="{StaticResource SidebarLabel}"/>
            <ComboBox x:Name="LanguageCombo" Style="{StaticResource SidebarCombo}" SelectedIndex="0" Margin="0,0,0,17">
              <ComboBoxItem x:Name="AutoLanguageItem" Content="自动（跟随系统）" Tag="Auto"/>
              <ComboBoxItem x:Name="ChineseLanguageItem" Content="简体中文" Tag="zh-CN"/>
              <ComboBoxItem x:Name="EnglishLanguageItem" Content="English" Tag="en-US"/>
            </ComboBox>

            <Grid Margin="0,0,0,14">
              <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock x:Name="DurationLabel" Text="运行时长（小时）" Style="{StaticResource SidebarLabel}"/>
                <TextBox x:Name="DurationText" Text="24" Style="{StaticResource InputTextBox}" ToolTip="输入 0 表示不限时"/>
              </StackPanel>
              <StackPanel Grid.Column="2">
                <TextBlock x:Name="IntervalLabel" Text="采样间隔（秒）" Style="{StaticResource SidebarLabel}"/>
                <TextBox x:Name="IntervalText" Text="15" Style="{StaticResource InputTextBox}"/>
              </StackPanel>
            </Grid>

            <TextBlock x:Name="UnitLabel" Text="速度单位" Style="{StaticResource SidebarLabel}"/>
            <ComboBox x:Name="UnitCombo" Style="{StaticResource SidebarCombo}" SelectedIndex="0" Margin="0,0,0,17">
              <ComboBoxItem x:Name="UnitMbItem" Content="MB/s（兆字节/秒）" Tag="MB/s"/>
              <ComboBoxItem x:Name="UnitMbpsItem" Content="Mbps（兆比特/秒）" Tag="Mbps"/>
            </ComboBox>

            <TextBlock x:Name="AdapterModeLabel" Text="网卡模式" Style="{StaticResource SidebarLabel}"/>
            <StackPanel Orientation="Horizontal" Margin="0,1,0,10">
              <RadioButton x:Name="AutoModeRadio" Content="自动（推荐）" Foreground="#F1F5FA" IsChecked="True" Margin="0,0,20,0"/>
              <RadioButton x:Name="ManualModeRadio" Content="手动" Foreground="#F1F5FA"/>
            </StackPanel>
            <TextBlock x:Name="AdapterModeHint" Text="自动合并所有已连接的物理网卡，并排除虚拟网卡。" Foreground="#8492A6" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <Border Background="#172437" BorderBrush="#2E3D52" BorderThickness="1" CornerRadius="10" Padding="5" Height="173">
              <ListBox x:Name="AdapterList" Style="{StaticResource SidebarAdapterList}">
                <ListBox.ItemTemplate>
                  <DataTemplate>
                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Foreground="White" Margin="6,5" VerticalContentAlignment="Top">
                      <StackPanel Margin="5,0,0,0" MaxWidth="235">
                        <TextBlock Text="{Binding Name}" Foreground="#F4F7FB" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Detail}" Foreground="#AAB8CC" FontSize="10" Margin="0,3,0,0" TextTrimming="CharacterEllipsis"/>
                      </StackPanel>
                    </CheckBox>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>
            </Border>
            <Button x:Name="RefreshAdaptersButton" Content="刷新网卡列表" Style="{StaticResource SecondaryButton}" Padding="11,7" Margin="0,8,0,17"/>

            <TextBlock x:Name="OutputFolderLabel" Text="结果保存位置" Style="{StaticResource SidebarLabel}"/>
            <Grid>
              <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="8"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
              <TextBox x:Name="OutputFolderText" Style="{StaticResource InputTextBox}" IsReadOnly="True"/>
              <Button x:Name="BrowseButton" Grid.Column="2" Content="选择" Style="{StaticResource SecondaryButton}" Padding="12,8"/>
            </Grid>
          </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2" BorderBrush="#2A3648" BorderThickness="0,1,0,0" Padding="22,17,22,20">
          <StackPanel>
            <Grid>
              <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
              <Button x:Name="StartButton" Content="开始记录" Style="{StaticResource BaseButton}" Height="44"/>
              <Button x:Name="StopButton" Grid.Column="2" Content="结束记录" Style="{StaticResource DangerButton}" Height="44" IsEnabled="False"/>
            </Grid>
            <TextBlock x:Name="SidebarStatusText" Text="准备就绪" Foreground="#8492A6" FontSize="11" Margin="2,11,0,0" TextWrapping="Wrap"/>
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel Margin="28,24,30,30">
        <Grid Margin="2,0,2,20">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <StackPanel>
            <TextBlock x:Name="MainTitleText" Text="网络流量监控" Foreground="#152033" FontSize="25" FontWeight="SemiBold"/>
            <TextBlock x:Name="MainSubtitleText" Text="记录电脑物理网卡的实时上传与下载速度" Foreground="#7D899C" FontSize="12" Margin="0,6,0,0"/>
          </StackPanel>
          <Border Grid.Column="1" Background="#EAF0FF" CornerRadius="15" Padding="13,7" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
              <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="#8B98AA" Margin="0,0,7,0" VerticalAlignment="Center"/>
              <TextBlock x:Name="StatusText" Text="未运行" Foreground="#52627A" FontSize="12" FontWeight="SemiBold"/>
            </StackPanel>
          </Border>
        </Grid>

        <Grid Margin="0,0,0,16">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.25*"/>
          </Grid.ColumnDefinitions>
          <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,8,0">
            <StackPanel>
              <TextBlock x:Name="CurrentDownloadLabel" Text="当前下载" Foreground="#778499" FontSize="12"/>
              <TextBlock x:Name="CurrentDownloadText" Text="0.000" Foreground="#315FD5" FontSize="28" FontWeight="SemiBold" Margin="0,11,0,2"/>
              <TextBlock x:Name="DownloadUnitText" Text="MB/s" Foreground="#9AA5B4" FontSize="11"/>
            </StackPanel>
          </Border>
          <Border Grid.Column="1" Style="{StaticResource Card}" Margin="8,0">
            <StackPanel>
              <TextBlock x:Name="CurrentUploadLabel" Text="当前上传" Foreground="#778499" FontSize="12"/>
              <TextBlock x:Name="CurrentUploadText" Text="0.000" Foreground="#12946D" FontSize="28" FontWeight="SemiBold" Margin="0,11,0,2"/>
              <TextBlock x:Name="UploadUnitText" Text="MB/s" Foreground="#9AA5B4" FontSize="11"/>
            </StackPanel>
          </Border>
          <Border Grid.Column="2" Style="{StaticResource Card}" Margin="8,0">
            <StackPanel>
              <TextBlock x:Name="ElapsedLabel" Text="已运行" Foreground="#778499" FontSize="12"/>
              <TextBlock x:Name="ElapsedText" Text="00:00:00" Foreground="#25324A" FontSize="25" FontWeight="SemiBold" Margin="0,13,0,3"/>
              <TextBlock x:Name="RemainingText" Text="尚未开始" Foreground="#9AA5B4" FontSize="11"/>
            </StackPanel>
          </Border>
          <Border Grid.Column="3" Style="{StaticResource Card}" Margin="8,0,0,0">
            <StackPanel>
              <TextBlock x:Name="SampleTrafficLabel" Text="样本与流量" Foreground="#778499" FontSize="12"/>
              <TextBlock x:Name="SampleCountText" Text="0 个样本" Foreground="#25324A" FontSize="20" FontWeight="SemiBold" Margin="0,14,0,5"/>
              <TextBlock x:Name="TotalTrafficText" Text="下载 0 MB · 上传 0 MB" Foreground="#9AA5B4" FontSize="11" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
          </Border>
        </Grid>

        <ProgressBar x:Name="RunProgress" Height="4" Minimum="0" Maximum="100" Value="0" Foreground="#4F7CFF" Background="#E1E7F0" BorderThickness="0" Margin="2,0,2,16"/>

        <Border Style="{StaticResource Card}" Height="300" Margin="0,0,0,16">
          <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Grid>
              <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
              <StackPanel>
                <TextBlock x:Name="ChartTitleText" Text="实时速度趋势" Foreground="#25324A" FontSize="15" FontWeight="SemiBold"/>
                <TextBlock x:Name="ChartSubtitleText" Text="最近 60 个采样点，纵轴会自动调整" Foreground="#929DAD" FontSize="10" Margin="0,4,0,0"/>
              </StackPanel>
              <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="8" Height="8" Fill="#4F7CFF"/><TextBlock x:Name="DownloadLegendText" Text="下载" Foreground="#6F7D90" FontSize="11" Margin="5,0,16,0"/>
                <Ellipse Width="8" Height="8" Fill="#20B486"/><TextBlock x:Name="UploadLegendText" Text="上传" Foreground="#6F7D90" FontSize="11" Margin="5,0,0,0"/>
              </StackPanel>
            </Grid>
            <Border Grid.Row="1" Background="#FBFCFE" CornerRadius="10" Margin="0,15,0,0" ClipToBounds="True">
              <ContentControl x:Name="ChartHost"/>
            </Border>
          </Grid>
        </Border>

        <Grid>
          <Grid.ColumnDefinitions><ColumnDefinition Width="1.45*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
          <Border Style="{StaticResource Card}" Height="254">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
              <StackPanel Margin="0,0,0,12">
                <TextBlock x:Name="RecentTitleText" Text="最近 5 条采样" Foreground="#25324A" FontSize="15" FontWeight="SemiBold"/>
                <TextBlock x:Name="RecentSubtitleText" Text="新记录显示在最上方" Foreground="#929DAD" FontSize="10" Margin="0,4,0,0"/>
              </StackPanel>
              <DataGrid x:Name="RecentGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" SelectionMode="Single" HeadersVisibility="Column" RowHeaderWidth="0" GridLinesVisibility="None" BorderThickness="1" BorderBrush="#E8EDF4" Background="White">
                <DataGrid.Columns>
                  <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="88"/>
                  <DataGridTextColumn Header="下载 MB/s" Binding="{Binding Download}" Width="105"/>
                  <DataGridTextColumn Header="上传 MB/s" Binding="{Binding Upload}" Width="105"/>
                  <DataGridTextColumn Header="网卡" Binding="{Binding Adapters}" Width="*"/>
                </DataGrid.Columns>
              </DataGrid>
            </Grid>
          </Border>

          <Border Grid.Column="2" Style="{StaticResource Card}" Height="254">
            <Grid>
              <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
              <StackPanel>
                <TextBlock x:Name="SummaryTitleText" Text="本次统计" Foreground="#25324A" FontSize="15" FontWeight="SemiBold"/>
                <TextBlock x:Name="SummaryStateText" Text="开始记录后显示统计结果" Foreground="#929DAD" FontSize="10" Margin="0,4,0,0"/>
              </StackPanel>
              <Grid Grid.Row="1" Margin="0,15,0,8">
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                <Grid.ColumnDefinitions><ColumnDefinition Width="88"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <TextBlock x:Name="MinLabelText" Text="最小速度" Foreground="#8793A5" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock x:Name="MinSpeedText" Grid.Column="1" Text="下载 —  ·  上传 —" Foreground="#334155" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock x:Name="MaxLabelText" Grid.Row="1" Text="最大速度" Foreground="#8793A5" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock x:Name="MaxSpeedText" Grid.Row="1" Grid.Column="1" Text="下载 —  ·  上传 —" Foreground="#334155" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock x:Name="AvgLabelText" Grid.Row="2" Text="平均速度" Foreground="#8793A5" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock x:Name="AvgSpeedText" Grid.Row="2" Grid.Column="1" Text="下载 —  ·  上传 —" Foreground="#334155" FontSize="12" VerticalAlignment="Center"/>
              </Grid>
              <Button x:Name="OpenResultsButton" Grid.Row="2" Content="打开结果文件夹" Style="{StaticResource LightButton}" Padding="13,8" Visibility="Collapsed" HorizontalAlignment="Stretch"/>
            </Grid>
          </Border>
        </Grid>

        <Border x:Name="SessionMessageBorder" Background="#EAF0FF" BorderBrush="#D6E1FF" BorderThickness="1" CornerRadius="12" Padding="16,12" Margin="0,16,0,0">
          <TextBlock x:Name="SessionMessageText" Text="设置参数后点击“开始记录”。CSV 会在每次采样后立即保存，结束时同时生成 Markdown 汇总。" Foreground="#536784" FontSize="11" TextWrapping="Wrap"/>
        </Border>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</Window>
